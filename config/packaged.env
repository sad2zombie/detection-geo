# 打包内置配置 — 构建安装包前按环境修改，目标机开箱即用（无需手动配置）
# 测试环境示例；正式包请替换为正式接口与 Kafka topic 后重新打包

# 拉取任务接口（待定，打包前填写）
CONSUMPTION_FETCH_URL=

# Kafka 结果回传（明文）
KAFKA_BOOTSTRAP_SERVERS=8.129.48.181:9094,120.24.45.182:9094,47.112.155.69:9094
KAFKA_RESULT_TOPIC=

# 消费轮询
CONSUMPTION_POLL_ENABLED=true
CONSUMPTION_POLL_INTERVAL=10

# 可选：LLM / 搜索 API（官网检测兜底）
# LLM_API_KEY=
# LLM_API_BASE=https://api.openai.com/v1
# LLM_MODEL=gpt-4o
